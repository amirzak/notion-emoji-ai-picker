{
  "name": "ai-emoji-suggester-for-notion",
  "version": "0.0.3",
  "description": "AI-powered emoji suggestions for Notion pages",
  "scripts": {
    "build": "node build.js",
    "build:dev": "node build.js --dev",
    "watch": "node build.js --dev --watch",
    "release": "npm run build && cd dist && zip -r ../build-v$npm_package_version.zip ."
  },
  "keywords": ["notion", "emoji", "chrome-extension", "ai"],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.20.1"
  }
}
